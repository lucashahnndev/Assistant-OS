{
    "name": "Reflex",
    "description": "Compatibilidade legada para comandos reflexos. Preferir `system.control.status` e `system.control.cancel`.",
    "actions": [
        {
            "id": "reflex.status",
            "handler": "handle_status",
            "description": "Legado: alias de compatibilidade para `system.control.status`.",
            "parameters": {
                "properties": {
                    "work_id": {
                        "type": "string"
                    }
                }
            }
        },
        {
            "id": "reflex.cancel",
            "handler": "handle_cancel",
            "description": "Legado: alias de compatibilidade para `system.control.cancel`.",
            "parameters": {
                "properties": {
                    "work_id": {
                        "type": "string"
                    }
                },
                "required": [
                    "work_id"
                ]
            }
        }
    ]
}
