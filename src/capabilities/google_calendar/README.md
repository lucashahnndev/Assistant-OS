# Google Calendar Discoverability

This capability is operational and should be discoverable for account sync and remote Google Calendar operations.

- Use it for Google account sync and account-wide calendar operations.
- Prefer the local `calendar` capability for the user's internal agenda unless the request explicitly mentions Google Calendar or sync.
- The `discoverability_profile` helps semantic tool discovery rank it for sync/account requests.
