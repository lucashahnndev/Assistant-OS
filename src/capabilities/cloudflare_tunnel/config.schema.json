{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "default": false,
      "title": "Enabled"
    },
    "auth_token": {
      "type": "string",
      "title": "Cloudflare Tunnel Token",
      "x-ui": {
        "placeholder": "Seu token do túnel (gerado no Zero Trust)"
      }
    },
    "target_port": {
      "type": "integer",
      "title": "Target Port",
      "description": "Port to expose (leave empty for default frontend port)",
      "x-ui": {
        "placeholder": "Ex: 8000"
      }
    },
    "domain": {
      "type": "string",
      "title": "Custom Domain",
      "description": "Seu domínio configurado no painel do Cloudflare Zero Trust (usado para exibir o link na interface)",
      "x-ui": {
        "placeholder": "Ex: atlas.meudominio.com"
      }
    }
  }
}
